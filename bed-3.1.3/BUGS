Field after repeat 8 bytes is place in front
Zoeken in regex in multiply niet goed.
Clone niet goed
Set repeat data type. Remove: filter in the way. Remove filter: no filter set.

android doesn't follow symlink. Make one myself.
Kill bed results in segmentation violation without core dump.

TOBUILDGCC just g++ after setting CXX to something else


Open van 0 length files, geeft zeer grootte file
mingw64:
	- cmd.com niet aangepast aan scherm grootte
	- verandere scherm grootte in andere terminal wordt niet verwerkt
	- cntr-break ends program, contr-C doet niets
	


in Memory::saveto etc moet afhankelijk van soort fout anders gehandeld worden: geen andere manier saven als disk full etc.


Floats worden zo 000004.722222 weergegeven
/proc/kcore heeft problemen aan het eind

Als in eerst twee velden in een duplicate heb, en een derde veld toevoegd wordt het aan de duplicaat toegevoegd

Hoe het directory verandere waarin het programmma uiteindelijk geinstalleerd wordt?
Zodat met make install prefix=dir dir een ander directory kan zijn waarin alleen tijdelijk in geinstalleerd wordt.

regex replace .*\b of .*\> stopt niet bij 0

Als een file niet geopend kan worden, is de file op een heel onhandige manier geselecteerd in filemanager.

arm64 reageert traag op toetsen
zegt: can't open .bedrc-bin for reading, DEBUG?
list zoeken niet in background. hoort zo # en x.
s onderbreekt niet altijd
termux package installeert niet
android arm32:
rpath?
	libc++shared.so nodig
termux zonder -D__ANDROID_API__=26 



linux: zonder bedrc werken home en end niet
android: m5: shell doesn't work
font set doesn't work with rxvt
title shows jka instead of root.
Plasma ziet enkel eerste letter selection.
opensuse konsole zonder bedrc: backspace
problemen met titleset. Niet gezet, fout melding bij root
searchclip niet goed, vind some data niet
showmarks 
dynamic libraries Windows

twee keer: Adjustable data format binary editor in deb
record niet goed
juiste opties android
debug messages moeten weg.
titleset?
verkeerde plaats
subtrace liep vastn


GetProcessTimes
in showmarks e.d. semaphores goed?
eindigd traag
na truncate marks over grens file
regex search in numbers matches spaties
re_search_2 overall goed?
searchinterrupt in backwards normal search is slow
unit wordt niet gesaved
meet opstart snelheid en gebruik dat voor:
	- verschillende showmarks
	- begin intervallen search
Delegating Constructors
help ander WINDOW?
Waarom vind ik copybuffer niet?
edit geen problemen?
Eerst keer replace regex geen reg selected
in dse.nl download niet directory
not searched bijhouden?
search begint soms niet direct
unit backwarch hyperscan show 2 units
na list search springen naar hit
Unit?
align springt terug
list aanzetten geeft eerste
list type fout bij /dev/sda


gat in edit menu
	voor next gebruikt enkel in bytesearch

Align instelling veranderd in single byte search

verwijder *.c* testfiles


align op sectorgrootte, niet blocksize

doe iets met overbodige argumenten offset e.d.

ascii search traag, ergens mis?
alle plugins aangepast op alignment?
is bytes het aantal bytes ook met filters?

  -zoeken schrijfbaar bestand? Android, Win, Linux
doesn't replace multiple hit in one unit
re2: formre2replace niet zo goed: bij grotere insert opschuiven oude data, in volgende veld. Zonder plakken oude gegevens.
e.g. ABC-> FFF\0. Oplossingen:
 - Op element grote afsnijden, uitvergroten?
 - grotere data:
 - wat doet gnu?

options kommen niet goed uit wanneer niet all regexes gecompileerd
subtractmarks doesn't delete all instances at a certain position

searchpos en interrupt
gnusearch voegt 1 2 3 4 5 niet goed in. plakt deel input aan 5 voor veranderen halve getallen
gnureplace werkt niet goed in formdata:
	- \1\1\1\1\1& niet goed
	- springt te ver door
re2 assumes [A-F][A-F]* gnu matches empty
hoe ver in anders geshifte stream zoeken na hit in andere stream?
re2 en gnu finden verschillende uitkomsten met (FF){3,4}, hyperscan 5 meer dan gnu
re2 list search zeer traag
Andere ook eindes in list?
re2 meer overlap geven
Profile program
gnuerror: In gnu geeft \b8([^ ]* [^ ]*){3,4}25\b  te korte hits
als regexcompile faalt is er geen regexexp, maar nextsearch wordt uitgevoerd


Viewbuf has different uses and functions that can't be intermixed.

altijd bs Stallman, other shift

test getblock return value
sectoralloc etc


wat met interrupt
regex search moet ook binnen eenheid tonen

geen error boodschap bij onleesbaar nummer

insert past gives error message about size and pasts I don't know what

rekening houden met filesize in Viewbuf
viewbuf nog niet goed view.view() aangeroepen start nog niet gezet?

Resize terminal while writing searchstr, removes it.
shift ook staat boundaries aan. pos+1?
Moet op boundaries blijven. Eerder stoppen in block als file groter?
OOk fout in niet regex?

aan te veel libraries gelinked?
bij digit niet-regex search: regex wordt vertaald in iets anders gezocht en gevonden
re2search
	- in contain: bij next blijft  bij op positie
	word begin etc klopt niet aan begin: tekens voor cursor tellen niet mee in match. 
	bij next search starten vanaf cursor of volgende match nemen?, in config?
	Linux:
		replace segmentation violation
		replace moet niet searchregs ander lib gebruiken
		int matchlen=searchregs->end[0]-searchregs->start[0];

replace: wat als replacestr kleiner is?
remove debug statements

Restrict to physical drive?
i686-pc-cygwin-g++ programs generen geen output
after switch to other tty, no sleep possible
wordt steeds *.bedda toegevoegd, verandere van dir gaat niet work with glob

lange namen worden niet volledig getoond. Toon in titel?
androidterminal: problemen bij niet root

for filters: file command

Mogelijkheden:
	-use disassembler lib

plaats files: bedrc etc
Contrl-Z gaat goed 

addmagic doesn't work


pdcurses: problem doesn't work with large sceen: e.g. font size 5 and maximal cmd.com window. Example programs supplied with pdcurses3.4 have the same problem

nbed doesn't work with cygwin1.dll in other place then /bin


iets met maximaal aantal drive/partitions doen.
export CYGWIN="$CYGWIN error_start=gdb -nw %1 %2"

export CYGWIN="$CYGWIN error_start=dumper -d %1 %2"

na mountvol d: /P wordt  kan volume wel geopend worden,maar wordt vreemd
weergegeven



Als save niet lukt en men eindigd met save yes krijgt men als antwoord Cancelled!


Linux:
lijnen bij lopen door menubar. Bij gebruik TERM=xterm. Niet bij TERM=rxvt of TERM=xterm-color

Windows:
	FSCTL_ALLOW_EXTENDED_DASD_IO wordt ook toegepast  op niet partitions
	schrijven aan end drive werkt niet bij kleine harddrive
	
Geen error checking op toepassingen Editor::fromfile ("editor.cpp"). Worden lees fouten goed behandeld?

linux:
	to grootte -d optie bij lijdt tot slechte weergaven ook op punten in device
	coredump
	Wat met bestanden die constant van grootte veranderen?


FreeBSD:	
	dd -conv
	time: strlen?, geen negatief jaar

Solaris: 
	27 79 80 =F1
	END en HOME don't work
	iets met disks tonen


Ubuntu: rare weergaven elf64 op gegeven moment	
	record keys met gecancelde end, lijkt toch te eindigen.
Bij veranderen naar ander scherm door kill soms twee processes actief

Field komen steeds voor datatype
64 int  Program::giveselection(XSelectionRequestEvent *req) { only odd members


vervang alle long voor int en remove what doesn't work
Kan enkel geheel compose datatype selecteren. Probleem: herordenende filters
Moeilijk binnen composed verplaatsen

bitflags niet echt gemakkelijk te editen.  Input moet op precieze plaats bit.

searchclip cursor staat in vorige unit in multibyte datatype
Bij gebruik -d optie tot te grote grootte, coredump bij exploreren niet bestaande ruimte.
tomode goed?

ga all %ld om te zien of het bij int gebruikt wordt on
kijk naar L bij ints

Waarom niet kopieren bij enkel selectie?
title verandert niet bij terug uit shell

Windows:

	Uittesten op USB sticks zonder filesysteem
	bij *** No mountpoints *** ook minder andere gegevens
	isfloppy integreren met andere CreateFile
	Wanneer er geen filesysteem  op disk staat krijg ik warning "THe Parameter is incorrect"
	Program Files\bed werkt niet bij andere drive letter.
	Adm: wordt getoond in Windows XP, goed?
	Test op floppy voor snelheid
	Ergens na filedata liep xp versie vast, INCDSE etc?
	tmp paden niet alleen maar in c: zoeken
	SHELL variable zetten gaat niet goed
	EDITOR gaat ook slecht
	vim.exe gaat niet, edit.com wel
	write zeurt over vinden file, ik kan file ook niet vinden. Notepad heeft geen problemen.

	uittesten zonder rest cygwin: hernoem c:\cygwin en andere computer


	bed loopt vast met cygwin1.dll in c:/windows als alle andere /bin/cygwin1.dll gebruiken.

	something wrong with: "Redirection is not supported." before "bed failed, execute c:/Program Files/bed/bedn.exe ..." ?


	"Why edit zero sized files?" when trying editing non-existing DVD 
	win32: 

INCDES overall? getfiles mvtakefile, arraysort glob etc?
mallocs goed?





Waarom worden files in in TEMP of tmp gevormd


Geeft Unknown error bij saven file, maar is wel gesaved?
Error doorgeven?


pdcurses black-white isn't always black and white
tmp file readonly geopend in vim
win95: bedrecord wordt niet verwijderd


	
Wat als in p*/ex*/title.cc GetProcAddress(advapi32,"CheckTokenMembership")) faalt?

Got "System Error: no child", can't replicate it.
install mv bed bed.bak lukt niet als bed.bak en bed.bak/bed bestaan
overwrites without warning?  Testen op geopend? Shared?
cygwin namen tmpfiles, moeten win namen worden?

wordt directory corrent gekozen voor macro, marks, datatype etc?

Add dontdesturbes around new code?
	DONTINTCURSES goed?



Cdrom device wordt na mislukte poging tot saven kleiner, volgens
nieuw opgestarte bed en errors aan einde file.
3580690432
Error message by pkg_delete
rpmbuild ends in error 'getOutputFrom(): Broken pipe' under redhat 9
Error message by pkg_delete


Windows:
Win95: als file.exe niet bestaat loopt bed past bij uitvoeren via filetype. Can het onder Vista niet reproduceren.

Als een nieuw dos window of xterm gestart wordt met start uit een shell vanuit bed blijft de boodschap "exit to return to bed" staan (PS1, PROMPT); Verschillende type environmental variables?

Prompt bij shell?

Wat doen met verwisselen USB stick
SL_FORCE_DIRECT_WRITE only from kernel mode

Lock before save doesn't work with disks.

Altijd dezelfde default 1 bytes file gebruiken leidt tot mogelijk access permission and Sharing probleem.


** misplaatsen realloc's? Geen behoefte aan behoud inhoud.
** Frees afgaan en kijken of altijd malloc vooraf ging

